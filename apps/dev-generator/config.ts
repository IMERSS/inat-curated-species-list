import type { GeneratorConfig } from '@imerss/inat-curated-species-list-generator';

const config: GeneratorConfig = {
  curators: ['benkeen'],
  taxonId: 7085,
  placeId: 53787,
  dataFilename: 'data.json',
  logFile: 'log.txt',
};

export default config;
